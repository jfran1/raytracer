use crate::utils::vectors::{ThreeVector, Color};
use crate::utils::rays::Ray;
use crate::utils::HitRecord;

pub struct Material {
    pub fn scatter(r_in: mut Ray, rec: &HitRecord, attenuation: &color, scattered &ray) -> bool {
        0        
    }
}

